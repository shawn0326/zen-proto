[package]
name = "zen-render"
version = "0.1.0"
edition.workspace = true
publish = false
description = "Domain-independent FrameGraph render host and frame composition contracts"
readme = "README.md"

[features]
default = []
snapshot = ["zenfg/snapshot"]

[dependencies]
zenfg.workspace = true
wgpu.workspace = true

[dev-dependencies]
wgpu = { workspace = true, features = ["noop"] }
