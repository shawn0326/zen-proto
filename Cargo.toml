[package]
name = "zen-proto"
version = "0.1.0"
edition = "2024"

[dependencies]
wgpu = "27"
glam = { version = "0.30", features = ["bytemuck"] }
bytemuck = { version = "1.24", features = ["derive"] }
wgpu-profiler = { version = "0.25" }

[dev-dependencies]
winit = "0.30"
pollster = "0.4"
glam = { version = "0.30", features = ["rand"] }
rand = "0.9"
